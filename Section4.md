# Project Jupyter

The Jupyter notebook was originally called IPython notebook and was a browser-based interface for Python. It has since turned into a browser-based interface for many languages including (**JU**)lia, (**PYT**)on (**E**)t **R**. 
